services:
  analista-instagram:
    container_name: analista-instagram
    image: analista-instagram-ai:latest
    build: .
    env_file:
      - .env
    volumes:
      - ./playwright/data:/app/playwright/data
      - ./playwright/.auth:/app/playwright/.auth
    command: npm run ig:collect
